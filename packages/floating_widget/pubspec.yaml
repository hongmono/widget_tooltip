name: floating_widget
description: "A draggable floating widget for Flutter that can be repositioned anywhere on screen with boundary constraints."
version: 0.0.1
homepage: https://github.com/hongmono/flutter-packages
repository: https://github.com/hongmono/flutter-packages/tree/main/packages/floating_widget

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
